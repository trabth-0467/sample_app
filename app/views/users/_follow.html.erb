<%= form_for current_user.active_relationships.build, url: relationships_path(locale: I18n.locale), remote: true do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit t("follow"), class: "btn btn-primary" %>
<% end %>
